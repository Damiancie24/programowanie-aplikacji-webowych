import {firebaseConfig} 
import * as firebase from '../node_modules/firebase/index';

export class Db{
    constructor() {
        this.initialalizeDb();

    }
    initialalizeDb(): void {
        firebase.
    }
}